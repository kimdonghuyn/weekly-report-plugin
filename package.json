{
  "name": "weekly-report-plugin",
  "version": "1.3.0",
  "description": "Weekly work report generator for Claude Code and Codex CLI — aggregates git commits, session activity, and manual logs into per-project reports.",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test"
  }
}
